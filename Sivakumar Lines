// This source code is subject to the terms of the Mozilla Public License 2.0 at https://mozilla.org/MPL/2.0/
// © iamkalai

//@version=5
indicator("Sivakumar Lines", overlay=true)

show1 = input.bool(defval=true)
show2 = input.bool(defval=true)
show3 = input.bool(defval=true)
show5 = input.bool(defval=true)
show8 = input.bool(defval=true)
show13 = input.bool(defval=true)
show21 = input.bool(defval=true)
show34 = input.bool(defval=true)
show55 = input.bool(defval=true)
show89 = input.bool(defval=true)
show144 = input.bool(defval=true)
show233 = input.bool(defval=true)
show377 = input.bool(defval=true)
show610 = input.bool(defval=true)
show987 = input.bool(defval=true)
show1597 = input.bool(defval=true)
show2584 = input.bool(defval=true)
show4181 = input.bool(defval=true)
show6765 = input.bool(defval=true)
show10946 = input.bool(defval=true)
show17711 = input.bool(defval=true)
show28657 = input.bool(defval=true)
show46368 = input.bool(defval=true)

plot(show1 ? 0.9 : na, color=color.green, title="1S")
plot(show1 ? 1.10 : na, color=color.red, title="1R")

plot(show2 ? 1.80 : na, color=color.green, title="2S")
plot(show2 ? 2.20 : na, color=color.red, title="2R")

plot(show3 ? 2.70 : na, color=color.green, title="3S")
plot(show3 ? 3.30 : na, color=color.red, title="3R")

plot(show5 ? 4.50 : na, color=color.green, title="5S")
plot(show5 ? 5.50 : na, color=color.red, title="5R")

plot(show8 ? 7.20 : na, color=color.green, title="8S")
plot(show8 ? 8.80 : na, color=color.red, title="8R")

plot(show13 ? 11.7 : na, color=color.green, title="13S")
plot(show13 ? 14.3 : na, color=color.red, title="13R")

plot(show21 ? 18.9 : na, color=color.green, title="21S")
plot(show21 ? 23.1 : na, color=color.red, title="21R")

plot(show34 ? 30.6 : na, color=color.green, title="34S")
plot(show34 ? 37.4 : na, color=color.red, title="34R")

plot(show55 ? 49.5 : na, color=color.green, title="55S")
plot(show55 ? 60.5 : na, color=color.red, title="55R")

plot(show89 ? 80.1 : na, color=color.green, title="89S")
plot(show89 ? 97.9 : na, color=color.red, title="89R")

plot(show144 ? 129.6 : na, color=color.green, title="144S")
plot(show144 ? 158.4 : na, color=color.red, title="144R")

plot(show233 ? 209.7 : na, color=color.green, title="233S")
plot(show233 ? 256.3 : na, color=color.red, title="233R")

plot(show377 ? 339.3 : na, color=color.green, title="377S")
plot(show377 ? 414.7 : na, color=color.red, title="377R")

plot(show610 ? 549 : na, color=color.green, title="610S")
plot(show610 ? 671 : na, color=color.red, title="610R")

plot(show987 ? 888.3 : na, color=color.green, title="987S")
plot(show987 ? 1085.7 : na, color=color.red, title="987R")

plot(show1597 ? 1437.3 : na, color=color.green, title="1597S")
plot(show1597 ? 1756.7 : na, color=color.red, title="1597R")

plot(show2584 ? 2325.6 : na, color=color.green, title="2584S")
plot(show2584 ? 2842.4 : na, color=color.red, title="2584R")

plot(show4181 ? 3762.9 : na, color=color.green, title="4181S")
plot(show4181 ? 4599.1 : na, color=color.red, title="4181R")

plot(show6765 ? 6088.5 : na, color=color.green, title="6765S")
plot(show6765 ? 7441.5 : na, color=color.red, title="6765R")

plot(show10946 ? 9851.4 : na, color=color.green, title="10946S")
plot(show10946 ? 12040.6 : na, color=color.red, title="10946R")

plot(show17711 ? 15939.9 : na, color=color.green, title="17711S")
plot(show17711 ? 19482.1 : na, color=color.red, title="17711R")

plot(show28657 ? 25791.3 : na, color=color.green, title="28657S")
plot(show28657 ? 31552.7 : na, color=color.red, title="28657R")

plot(show46368 ? 41731.2 : na, color=color.green, title="46368S")
plot(show46368 ? 51004.8 : na, color=color.red, title="46368R")